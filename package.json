{
  "name": "chaos-study-room",
  "version": "1.0.0",
  "description": "CBSE Board Pomodoro, study log, syllabus tracker and aura farming dashboard",
  "main": "index.html",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "build:android": "npm run build && npx cap add android && npx cap build android",
    "sync:android": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.4.0",
    "@capacitor/android": "^5.4.0",
    "@capacitor/splash-screen": "^5.4.0",
    "@capacitor/status-bar": "^5.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.4.0"
  }
}
